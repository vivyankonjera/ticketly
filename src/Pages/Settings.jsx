import BackButton from "../Components/BackButton";

const Settings = () => {
    return (
        <div className='page'>
            <h3>Settings</h3>
            <BackButton />
        </div>
    );
};

export default Settings;
